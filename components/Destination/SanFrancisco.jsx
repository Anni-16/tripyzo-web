import DestinationContent from "./DestinationContent";
import { destinations } from "./destinationData";

const SanFrancisco = () => {
  return <DestinationContent destination={destinations.sanFrancisco} />;
};

export default SanFrancisco;
