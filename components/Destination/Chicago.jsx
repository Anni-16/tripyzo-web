import DestinationContent from "./DestinationContent";
import { destinations } from "./destinationData";

const Chicago = () => {
  return <DestinationContent destination={destinations.chicago} />;
};

export default Chicago;
