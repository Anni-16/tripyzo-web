import DestinationContent from "./DestinationContent";
import { destinations } from "./destinationData";

const Boston = () => {
  return <DestinationContent destination={destinations.boston} />;
};

export default Boston;
