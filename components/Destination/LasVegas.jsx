import DestinationContent from "./DestinationContent";
import { destinations } from "./destinationData";

const LasVegas = () => {
  return <DestinationContent destination={destinations.lasVegas} />;
};

export default LasVegas;
